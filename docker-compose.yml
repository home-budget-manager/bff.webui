name: home-budget-manager-bff.webui-local

services:
  bff-webui:
    image: home-budget-manager/bff.webui:local
    build:
      context: .
      dockerfile: Api.Dockerfile
    restart: always
    ports:
      - "5001:8080"
